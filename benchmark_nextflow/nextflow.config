report.overwrite  = true

workDir        = 'work'
projectDir     = './'
publishDir     = './output/'

params {
    conda_prefix = "\$CONDA_PREFIX"
    threads      = 2
    memory       = 32.GB
    default_conda_env = "envs/benchmarking_env.yml"
}

process {    
    container = "sowpati/ont-methylation-benchmarking:latest"

    cpus = params.threads
    memory = params.memory
    time = 3.d
    maxForks = 1
    
    resourceLimits = [
        cpus: params.threads,
        memory: params.memory,
        accelerator: 2
    ]

    withLabel: gpu {
        accelerator = 2
        cpus = params.threads
        memory = params.memory
        maxForks = 1
        beforeScript = """
            LOCK_FILE="/tmp/nextflow_gpu_job.lock"
            while ! mkdir "\$LOCK_FILE" 2>/dev/null; do
                echo "Waiting for other GPU job to complete..."
                sleep 10
            done
            trap 'rmdir "\$LOCK_FILE"' EXIT
        """
    }

    withLabel: cpu {
        cpus = params.threads
        memory = params.memory
        maxForks = 4
    }

    withLabel: download {
        cpus   = params.threads
        memory = params.memory
        maxForks = 4
    }

    withLabel: "std_conda" {
        conda = "${projectDir}/${params.default_conda_env}"
    }
    withLabel: 'rockfish' {
        conda = "${params.conda_prefix}/envs/${params.toolConfig.rockfish.conda}"
    }

    withLabel: "deepmod2" {
        conda = "${params.conda_prefix}/envs/${params.toolConfig.deepmod2.conda}"
        container = null
    }
    
    withName: 'deepbam_call' {
        conda = "${params.conda_prefix}/envs/${params.toolConfig.deepbam.conda}"
    }
    withName: 'deepplant_call' {
        conda = "${params.conda_prefix}/envs/${params.toolConfig.deepplant.conda}"
    }
}

profiles {
    apptainer {
        apptainer {
            enabled = true
            autoMounts = true
            runOptions = '--nv'
            autoMounts = true
        }
        docker.enabled = false
        conda.enabled = false
    }
    singularity {
        apptainer {
            enabled = true
            autoMounts = true
            runOptions = '--nv'
            autoMounts = true
        }
        docker.enabled = false
        conda.enabled = false
    }
    docker {
        docker {
            enabled = true
            runOptions = "--user \$(id -u):\$(id -g) --group-add 100 --gpus all"
        }

        apptainer.enabled = false
        conda.enabled = false
    }
    conda {
        conda.enabled = true
        docker.enabled = false
        apptainer.enabled = false
    }
    cluster {
        process.maxForks = 2
        process.cpus=25
        process.executor="pbs"
        process.queue="cpu-q"
        process.memory=110.GB
        process.time=20.d
        process.clusterOptions=""
        process.resourceLimits = [
            memory: 250.GB,
            cpus: 56,
            time: 30.d,
            accelerator: 2
        ]
    }
}
